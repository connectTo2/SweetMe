/* --------------------------------- require -------------------------------- */
const { getUserData } = require('./common');

/* --------------------------------- handler -------------------------------- */
const getWords = () => {};

const addWord = () => {};

const editWord = () => {};

const removeWord = () => {};

module.exports = {
  getWords,
  addWord,
  editWord,
  removeWord,
};
