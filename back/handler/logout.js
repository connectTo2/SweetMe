/* --------------------------------- require -------------------------------- */
const { getUserData } = require('./common');

/* --------------------------------- handler -------------------------------- */
const logOut = (req, res) => {};

module.exports = { logOut };
